# Multi-Supplier Stay Aggregator — Agent Guide

여러 외부 숙박 공급사의 상품을 하나의 표준 모델로 통합해 검색을 제공하는 백엔드다.
Java 21 + Spring Boot 4, Gradle 멀티모듈.

```
app              HTTP 진입점, Composition Root, 예외 핸들러, 스케줄러
application      유스케이스, Port 인터페이스        ← Spring 의존 없음
domain           표준 모델과 순수 규칙              ← 의존성 0
storage          PostgreSQL Adapter, Flyway
supplier-client  공급사 WebClient, DTO, Normalizer (ACL)
mock-supplier    별도 프로세스(:9090)               ← 본 코드와 물리적 분리
```

---

## 작업 방식

이 문서는 코드 작성·수정 과정에서 개발 보조 도구에 적용하는 프로젝트 기준을 정리한다.

### 1. 모든 판단에 선택지와 잃는 것

최소 2~3개의 대안을 표로 비교하고, 하나를 고른 뒤 **무엇을 잃는지** 명시한다.
**"하지 않는다"를 항상 후보 한 행으로 넣는다.** 미도입이 후보에 없으면 무언가는 반드시 추가된다.

```
correctness       틀린 값을 낼 수 있는가
failure 의미      실패했을 때 그것이 무엇을 뜻하는지 구분 가능한가
운영 복잡도       새로 관리해야 할 대상이 생기는가
구현 비용         코드·테스트 표면이 얼마나 늘어나는가
현재 범위 적합성   지금의 요구가 이것을 정당화하는가
```

### 2. 오버엔지니어링 판단

```
1. 측정된 병목이 없는데 성능을 위해 추가하는 것은 오버엔지니어링이다.
2. 계약이 보장하지 않는 사실을 전제로 삼는 설계는 그 자체가 결함이다.
3. 오류처럼 보인다는 직감으로 외부 계약에 없는 불변식을 만들지 않는다.
4. 미래 요구를 위한 추상화를 지금 만들지 않는다. 현재 카디널리티가 1:1이면 계층을 나누지 않는다.
```

기술을 아는 것과 지금 쓰는 것은 다르다.
Java 21을 고르고 Virtual Thread를 안 쓰는 것, Flyway를 쓰되 마이그레이션 이력을 만들지
않는 것이 그 예다.

### 3. 확인하지 않은 것을 단정하지 않는다

기본값·API 존재 여부·프레임워크 의미론은 확인 전까지 **"확인 필요"**로 표시한다.
확인하지 않은 버전은 `TBD`로 두어 **빌드를 깨뜨린다.**

이 원칙이 이 저장소에서 실제로 잡아낸 것들이 있다.

- Reactor Netty 의 커넥션 획득 대기 기본값이 45초라는 것 (우리 응답 타임아웃의 22배)
- `PoolAcquireTimeoutException` 이 `TimeoutException` 을 상속해서 **우리 풀 고갈이
  공급사 타임아웃으로 집계되고 있었다는 것**
- `@ConfigurationProperties` 가 미해석 플레이스홀더를 예외 없이 문자열로 넣는다는 것
  (`@Value` 와 다르다) — "설정이 없으면 기동이 실패한다" 는 문장이 틀렸다
- Resilience4j 가 연산자 순서를 규정하지 않는다는 것 — 단정해 적어 뒀다가 결론을 바꿨다

### 4. 검증이 실제로 검증하는지 확인한다

> 검증을 설계할 때 **"이 검증이 실패하려면 무엇이 잘못되어야 하는가"**를 먼저 묻는다.
> 답이 "잘못돼도 실패하지 않는다"면 그 검증은 없느니만 못하다. 통과했다고 믿게 만든다.

**병렬성·서킷·상한을 경과 시간으로 검증하지 않는다.** 응답이 모든 브랜치를 기다리므로
총 소요는 어떤 구조든 비슷하고, 격리가 깨져 있어도 `time curl` 은 통과한다.
동시 진입 수나 upstream 호출 수를 직접 센다.

주요 회귀선은 **일부러 망가뜨려 깨지는 것을 확인**한다.
README 의 테스트 섹션에 확인 기록이 있다.

### 5. 장애의 순간을 반드시 다룬다

다단계 상태 변경이 있으면 **각 단계 사이에서 프로세스가 종료되는 경우**를 전부 적는다.
파생 데이터를 도입한다면 **그것이 유실될 때**를 반드시 다룬다.

이상적인 결론은 "보상 코드가 필요 없다"이고, 그것은 복구 코드를 잘 짜서가 아니라
**부분 상태 자체를 만들지 않아서** 얻어진다. 카탈로그 단일 트랜잭션이 그 예다.

---

## 아키텍처 규칙

```
app ──→ application, storage, supplier-client, domain
application ──→ domain, reactor-core
storage ──→ application, domain
supplier-client ──→ application, domain
domain ──→ 없음
```

`domain` 의 `build.gradle.kts` 에는 `dependencies` 블록이 없다.
Spring 을 import 하면 ArchUnit 이전에 **컴파일이 깨진다.**

`application` 에만 `java-library` 를 적용한다. 포트 시그니처에 `Mono` 와 도메인 타입이
노출되므로 `api` configuration 이 필요하다.

**계층 깊이를 균일하게 적용하지 않는다.** `domain` 은 애노테이션 0, `application` 도 0
(app 이 `@Bean` 으로 조립), `storage` 는 `@Component` 를 유지한다.
어댑터는 정의상 프레임워크와 붙는 계층이고, 거기서 애노테이션을 빼면 순수성을 얻는 게
아니라 빈을 못 찾게 될 뿐이다.
**순수성을 원칙이 아니라 목적에 따라 적용한다.**

인프라 예외를 상위 계층으로 올려보내지 않는다. 포트 경계에서 번역한다 —
`DataAccessResourceFailureException` 이 컨트롤러까지 가면 포트 경계가 의미를 잃는다.

이 규칙들은 ArchitectureTest에서 자동으로 검증한다. **규칙을 완화하는 방식으로 위반을 해결하지 않는다.**

---

## 구현 규칙

**공급사별 분기를 오케스트레이션이나 도메인에 두지 않는다.** 도메인 규칙이 공급사에 따라
달라지면 그것 자체가 ACL 실패다. `SupplierId` 가 enum 이 아닌 이유도 같다.

**외부 DTO 는 전부 boxed, 도메인은 primitive.** primitive 면 Jackson 이 필드 부재를
`false`/`0` 으로 채우고, 그러면 "공급사가 아무 말도 안 했다" 가 "공급사가 선언했다" 로 둔갑한다.

**무손실 방향으로만 변환한다.** 일별 → 총액 합산은 계약이 보장하지만 총액 → 일별 분해는
불가능하다. 없는 정보를 만들어내지 않고 `Optional.empty()` 로 명시한다.
`null` 로 내리되 `@JsonInclude(NON_NULL)` 로 필드를 빼지 않는다 —
"모른다" 가 "필드 없음" 으로 흐려진다.

**정규화는 순수 함수다.** 지표를 직접 올리지 않고 진단을 결과값으로 반환한다.
기록은 어댑터가 `SearchTelemetry` 포트로 한다.

**검색 경로에서 `block()` 을 호출하지 않는다.** 카탈로그 동기화만 예외다(스케줄러 스레드).
정규화는 스케줄러 전환 없이 Netty 이벤트 루프에서 돌므로, 여기서 블로킹하면 그 루프에
물린 다른 공급사의 응답 처리까지 멈춘다.

**`onErrorResume` 은 `flatMap` 안쪽이다.** 바깥이면 첫 배치 실패가 나머지를 취소한다.
**`Flux.merge` 가 아니라 `flatMap(_, n)`** — 동시성 상한이 인자로 명시된다.
**`transform` 이 아니라 `transformDeferred`** — 조립 시점에 한 번만 평가되면 상한이
안 걸리는데 코드는 걸린 것처럼 보인다.

마이그레이션은 `V1__catalog.sql` 하나만 유지하고 계속 수정한다. 운영 DB 가 없으므로
이력의 이득 없이 읽는 비용만 늘어난다. 스키마를 바꾸면 `docker compose down -v`.

---

## 에러 규칙

**소유권과 재시도 가능성을 분리한다.**

```
INTERNAL_ERROR    코드가 틀렸다.   다시 보내도 같다.        500
LOCAL_SATURATION  자원이 부족했다. 잠시 뒤엔 될 수 있다.    503
```

500 은 "재시도하지 마라" 라는 뜻이므로 일시적 용량 문제를 500 으로 보고하지 않는다.

**공급사 성공률 지표에 우리 문제가 섞이면 그 지표는 아무 의미가 없다.**
공급사 4xx 는 공급사가 정상 동작하면서 "네 요청이 잘못됐다" 고 답한 것이므로 우리 잘못이다.

**502/504 는 동질 실패일 때만 쓴다.** 혼합에서 어느 하나를 우선할 근거가 없다.

**"쓸 만한 공급사가 있는가" 를 `offers.isEmpty()` 로 판정하지 않는다.**
전 숙소가 만실인 성수기에 503 이 나간다. 상태로 판정한다.

클라이언트가 고칠 수 있는 오류만 상세를 알려준다. 내부 오류의 상세는 알려줘도
할 수 있는 일이 없고 내부 구조·경로·SQL 만 샌다.

---

## 완료 기준

```bash
./gradlew spotlessApply && ./gradlew clean check
```

동시성·실패 분류·트랜잭션에 영향을 주는 변경은 테스트를 추가하기 전까지 완료가 아니다.

**성능이나 영향에 대한 주장은 측정으로만 한다.** 근거가 없으면 측정하거나 주장을 지운다.
측정 기록은 `docs/MEASUREMENTS.md` 와 `docs/measurements/` 에 둔다.
**측정 전에는 절대 MEASURED 라고 쓰지 않는다.**

문서에 쓴 주장은 코드와 테스트로 증명한다. 증명할 수 없으면 문서에서 지운다.
README 에 빈칸을 두지 않는다 — 설계만 한 항목은 **(설계)** 로 명시한다.

---

## 공개 저장소 규칙

특정 회사명이나 외부 안내 문서를 직접 가리키는 표현을 코드·문서·커밋 메시지에 두지 않는다.
`scripts/check-repo-hygiene.sh` 가 pre-commit 과 CI 에서 검사한다.

테스트를 추가하거나 지우면 README 의 테스트 집계표도 같이 고친다.
`scripts/check-doc-test-counts.sh` 가 CI 에서 실제 실행 결과와 대조한다.

---

## 용어 정의

| 용어 | 의미 |
|---|---|
| 숙소(property) | 공급사가 취급하는 하나의 숙박 시설 |
| 객실 타입(room type) | 숙소 안의 판매 단위. **코드는 해당 숙소 안에서만 유일하다** |
| 카탈로그 | 숙소·객실 타입 목록. 정적 데이터. 우리 DB 에 저장한다 |
| live | 요금·재고. 매번 바뀌며 저장하지 않는다 |
| 스냅샷 | 공급사가 준 카탈로그 전체 목록. 부분 적용하지 않는다 |
| 배치 | 공급사 벌크 조회 1회 단위. 최대 50개 (CONTRACT) |
| 정규화 | 공급사 응답을 표준 모델로 옮기는 것. 순수 함수 |
| 부분 실패 | 일부 공급사·배치가 실패해도 나머지로 응답하는 것 |
| 포화 | 우리 자원의 한계. 공급사 장애가 아니다 |

---

## 상세 Skill

Codex용 Skill:

- 코드 배치와 소유권: `.codex/skills/structure/SKILL.md`
- 새 공급사 추가: `.codex/skills/supplier/SKILL.md`
- 실패 분류와 격리: `.codex/skills/failure/SKILL.md`
- 숫자에 근거 붙이기: `.codex/skills/numbers/SKILL.md`

Claude Code용 Skill:

- 코드 배치와 소유권: `.claude/skills/structure/SKILL.md`
- 새 공급사 추가: `.claude/skills/supplier/SKILL.md`
- 실패 분류와 격리: `.claude/skills/failure/SKILL.md`
- 숫자에 근거 붙이기: `.claude/skills/numbers/SKILL.md`

Codex 작업에서는 `.codex/skills` 를, Claude Code 작업에서는 `.claude/skills` 를 우선 참고한다.
두 디렉토리의 내용은 동일하게 유지한다. 한쪽 도구만 써도 같은 기준이 적용되어야 하기 때문이다.

한쪽 skill 을 고쳤으면 다른 쪽도 같이 고친다.
`scripts/check-agent-guides.sh` 가 pre-commit 과 CI 에서 대조하므로, 갈라진 채로는 커밋되지 않는다.
